$("form").submit(function)) {}
// same as
$("form").on("submit", function() {}

$("form").off("submit"); {}

$("form").off();

// you can put anything on the end of a firebase URL 
// and as long as you use the same URL later it will take you to the 
// information that was originally in it

fill out 2 functions
each time a new message is added first function will execute 
and it will as for addtional objects when the page is reloaded

block of code executed 1x per message

append a string that is an HTML